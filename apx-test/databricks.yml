bundle:
  name: mirion-test-

sync:
  include:
    - .build

artifacts:
  default:
    build: uv run apx build

resources:
  apps:
    mirion-test--app:
      name: "mirion-test-"
      description: "mirion-test- created with apx"
      source_code_path: ./.build

targets:
  dev:
    mode: development
    default: true