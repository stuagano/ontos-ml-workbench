name: "Stage Dataset Events Workflow"
description: "Stage dataset change events for downstream processing."
format: "MULTI_TASK"
tasks:
  - task_key: "stage_dataset_events"
    spark_python_task:
      python_file: "stage_dataset_events.py"
    existing_cluster_id: "cluster-id" 

tags:
  owner: "ontos"